import {asyncHandler} from "../utils/asyncHandler.js";

const registerUser = asyncHandler(async (req, res) => {
    // get user details from Frontend
    // take all the required fields and setup
    // validation of not empty
    // check if already existing from username and email
    // check do we have avatar and file image
    // then upload them to cloudinary
    // add the URL given from cloudinary


    // CREATE USER OBJECT AND CREATE ENTRY 
    // Remove password and refresh token field
    // check for user creation and return the response 

} )



export {registerUser}